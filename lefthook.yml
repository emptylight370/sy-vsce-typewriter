pre-commit:
  parallel: true
  jobs:
    - name: Generate changelog
      skip:
        # 仅在修改了version的时候执行
        - run: $(! git diff --cached -U0 'plugin.json' | grep -q -F 'version')
      run: mise run changelog
      fail_text: "请检查依赖安装状态，运行mise install安装依赖"

    - name: Format code with prettier
      run: prettier -l {staged_files} | xargs -r prettier -w
      stage_fixed: true
      glob:
        - "*.md"
        - "*.json"
        - "src/*"
      fail_text: "请检查prettier安装状态，运行mise install安装依赖"

commit-msg:
  jobs:
    - name: Conventional commit
      script: commitmsg.sh
      runner: bash
# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#
# pre-commit:
#   parallel: true
#   jobs:
#     - run: yarn eslint {staged_files}
#       glob: "*.{js,ts,jsx,tsx}"
#
#     - name: rubocop
#       glob: "*.rb"
#       exclude:
#         - config/application.rb
#         - config/routes.rb
#       run: bundle exec rubocop --force-exclusion {all_files}
#
#     - name: govet
#       files: git ls-files -m
#       glob: "*.go"
#       run: go vet {files}
#
#     - script: "hello.js"
#       runner: node
#
#     - script: "hello.go"
#       runner: go run
